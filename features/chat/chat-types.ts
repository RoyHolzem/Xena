/**
 * Re-exports from @/lib/types for backward compat.
 * Import from @/lib/types directly in new code.
 */
export type {
  Role,
  ChatMessage,
  PresenceState,
  AvatarState,
  XenaActionCategory,
  XenaActionVerb,
  XenaActionEvent,
} from '@/lib/types';
